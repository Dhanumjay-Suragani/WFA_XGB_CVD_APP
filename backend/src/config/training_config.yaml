# src/config/training_config.yaml

training:
  test_size: 0.2
  val_size: 0.1
  stratify: true
  random_state: 42

evaluation:
  metrics:
    - accuracy
    - precision
    - recall
    - f1_score
    - roc_auc
