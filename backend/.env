# MongoDB Atlas → Database Access → pick this USER → use its PASSWORD (secret), not the username string.

MONGO_USER=dhanumjaysuragani1_db_user
MONGO_PASSWORD=dhanumjaysuragani
MONGO_CLUSTER_HOST=cvd.omfum8a.mongodb.net

MONGO_DB_NAME=cvd_db
CORS_ORIGINS=http://localhost:3000
TESSERACT_CMD=C:\Program Files\Tesseract-OCR\tesseract.exe
TESSDATA_PREFIX=C:\Program Files\Tesseract-OCR\tessdata
HOST=127.0.0.1
PORT=5000
FLASK_DEBUG=true

# Admin portal — change all three before use; ADMIN_TOKEN must be a long random string (API bearer).
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=password
ADMIN_TOKEN=replace-with-long-random-secret
