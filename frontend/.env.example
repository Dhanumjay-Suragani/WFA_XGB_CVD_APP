# Optional. If unset in development, requests use CRA proxy → Flask :5000 (see package.json).
# For production build, set this to your API origin, e.g.:
# REACT_APP_API_BASE_URL=http://127.0.0.1:5000
