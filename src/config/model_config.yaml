# src/config/model_config.yaml

xgboost:
  n_estimators: 300
  max_depth: 4
  learning_rate: 0.05
  subsample: 0.8
  colsample_bytree: 0.8
  eval_metric: logloss
  random_state: 42

focal_loss:
  alpha: 0.25
  gamma: 2.0

wfa:
  lambda: 0.6
